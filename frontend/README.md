# Frontend

React, TypeScript, and Vite frontend for the local chess UI.

Use the repository root [README.md](../README.md) for setup, run, and test instructions.

Frontend-specific commands:

```bash
npm test
npm run build
npm run dev -- --host 0.0.0.0 --port 5173
```
